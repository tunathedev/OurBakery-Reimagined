{
  "rules": {
    "ob": {
      ".read": "auth != null",
      ".write": "auth != null",
      "ledger": {
        "$stream": {
          "$day": {
            "$eventId": {
              ".write": "!data.exists()"
            }
          }
        }
      }
    }
  }
}
